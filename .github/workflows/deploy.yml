# Disabled: production is on Vercel (@sveltejs/adapter-vercel), not GitHub Pages.
# This workflow expected a static `build/` output from adapter-static, which this
# project no longer produces — so the job always failed after `npm run build`.
#
# To re-enable Pages later, switch svelte.config.js back to adapter-static and
# restore the build/deploy steps below.
#
# name: Build and Deploy
# on:
#   push:
#     branches:
#       - master
#       - main

name: Build and Deploy (disabled)
on:
  workflow_dispatch: # manual only; does not run on push
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: echo "GitHub Pages deploy is disabled. Use Vercel for production deploys."
